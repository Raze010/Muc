<?php

namespace App\Back\Graphique;

class GenGraphique {

    public function generer($largeur, $hauteur, $dateDebut, $prixDebut, $dateFin, $prixFin){



        // // Créer une image vide de 200x100 pixels
        // $image = imagecreatetruecolor($largeur, $hauteur);

        // // Allouer des couleurs
        // $blanc = imagecolorallocate($image, 255, 255, 255);
        // $noir = imagecolorallocate($image, 0, 0, 0);

        // // Remplir l'image avec du blanc
        // imagefilledrectangle($image, 0, 0, 500, 100, $blanc);

        // // Écrire du texte noir
        // imagestring($image, 5, 50, 40, 'Bonjour !', $noir);

        // // Afficher l'image
        // imagepng($image);

        // // Libérer la mémoire
        // imagedestroy($image);
    }
}